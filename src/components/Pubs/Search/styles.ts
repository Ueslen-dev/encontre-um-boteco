import styled from 'styled-components';

export const Wrapper = styled.main`
  margin: 0 0 50px 0;
  display: flex;

  .icon {
    font-size: 20px;
    position: relative;
    right: 40px;
    top: 35px;
    color: var(--grey-color);
    cursor: pointer;
  }
`;
